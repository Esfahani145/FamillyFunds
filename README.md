این پروژه یک استارتر ساده است برای صندوق قرض‌الحسنه خانوادگی:
- بک‌اند: Django + Django REST Framework + Simple JWT
- فرانت‌اند: React (create-react-app style)

مسیرها:
- /backend -> کد Django
- /frontend -> کد React

روش اجرا در لوکال در فایل راهنما (در خروجی اصلی) توضیح داده شده است.
